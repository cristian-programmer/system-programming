#include <assert.h>

#include "./../linkedlist.h"


int main(void){
	struct list  node* = NULL;
	unsigned size = sizeof(int);

	int arr[] = {10, 20, 30, 40, 50}, i;
	for(i=4; i >= 0; i--){
		push(&node, &arr, size);
	}
	printList(start, prin)

}

