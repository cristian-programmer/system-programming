TITLE EMUPROGRAM
DATA SEGMENT
DATA ENDS

STACK SEGMENT 
    DB 64 DUP(0)
STACK ENDS

CODE SEGMENT
    ; ATTR NEAR AND FAR
    MAIN PROC FAR
    ASSUME DS: DATA, CS:CODE, SS:STACK
    PUSH DS
    MOV AX, 0
    PUSH AX

    MOV AX, DATA
    MOV DS, AX
    MOV ES, AX
    RET 
MAIN ENDP 
CODE ENDS
END MAIN